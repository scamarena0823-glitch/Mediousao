name: Add Próximamente feature
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy Próximamente patch disabled; ready-flow.js is the active customer flow."