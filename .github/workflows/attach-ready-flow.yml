name: Attach MEDIOUSAO ready flow
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy ready-flow patch disabled. MEDIOUSAO ready flow is maintained directly in ready-flow.js."