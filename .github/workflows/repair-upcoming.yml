name: Repair Próximamente y reservas
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy repair workflow disabled; ready-flow.js is the active customer flow."